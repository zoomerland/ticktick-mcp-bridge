# Agent Handoffs

Store durable handoffs here when an agent completes a bounded task.

Use one Markdown file per handoff. Prefer names like:

```text
YYYY-MM-DD-agent-topic.md
```

Do not commit secrets, raw tokens, private task exports, or logs containing
credentials.
